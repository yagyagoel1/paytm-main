export const Button = ({label})=>{
    return <div>
        <button 
        className="w-full my-5 mx-5 py-2 border rounded-lg
         bg-black text-white font-semibold shadow-md">{label}</button>
    </div>
}